# Data instructions

Raw datasets are not bundled in this repository.

The current code resolves its input folders relative to `src/`, so download the datasets and place them under:

```text
src/datasets/
```

See `src/datasets/README.md` for the exact directory layout and source information.
